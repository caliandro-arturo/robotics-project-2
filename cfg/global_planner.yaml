
GlobalPlanner:

  grid_resolution: 0.1
  movement_cost_weight: 1.0
  min_obstacle_dist: 0.5
  search_algorithm: astar
  AStar:
    heuristic_weight: manhattan
    motion_model: 8dir

map:
  frame_id: map
  size_x: 10.0
  size_y: 10.0
  resolution: 0.05

topic:
  path:   #topic per la pubblicazione del percorso "/global_planner/path"
